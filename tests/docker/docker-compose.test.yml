services:
  mysql57:
    image: mysql:5.7
    ports:
      - "127.0.0.1:3357:3306"
    environment:
      MYSQL_ROOT_PASSWORD: testpass
      MYSQL_DATABASE: testdb
    volumes:
      - ./init-test-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    command: >
      mysqld
      --performance-schema=ON
      --performance-schema-consumer-events-statements-history=ON
      --performance-schema-consumer-events-statements-history-long=ON
      --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -ptestpass --silent"]
      interval: 5s
      timeout: 5s
      retries: 20

  mysql80:
    image: mysql:8.0
    ports:
      - "127.0.0.1:3380:3306"
    environment:
      MYSQL_ROOT_PASSWORD: testpass
      MYSQL_DATABASE: testdb
    volumes:
      - ./init-test-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    command: >
      mysqld
      --performance-schema=ON
      --performance-schema-consumer-events-statements-history=ON
      --performance-schema-consumer-events-statements-history-long=ON
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -ptestpass --silent"]
      interval: 5s
      timeout: 5s
      retries: 20

  mysql84:
    image: mysql:8.4
    ports:
      - "127.0.0.1:3384:3306"
    environment:
      MYSQL_ROOT_PASSWORD: testpass
      MYSQL_DATABASE: testdb
    volumes:
      - ./init-test-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    command: >
      mysqld
      --performance-schema=ON
      --performance-schema-consumer-events-statements-history=ON
      --performance-schema-consumer-events-statements-history-long=ON
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -ptestpass --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
