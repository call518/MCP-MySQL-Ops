name: mcp-mysql-ops
description: "MCP server for MySQL"
homepage: https://github.com/call518/MCP-MySQL-Ops
license: MIT
tags:
  - airflow
  - dag
  - cluster
  - monitoring
  - mcp

runtime: "python"

# Configure connection secrets via the Smithery dashboard environment settings
# (e.g. MYSQL_HOST, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE).
